name: "Lint GitHub Actions workflows"

on:
  pull_request:
    paths:
      - ".github/**"

jobs:
  actionlint:
    uses: "./lint-actions.yml@8.3"
    with:
      skip-nasm-install: true
      skip-libimagequant-install: true
